[package]
name = "sez-hotkey"
version.workspace = true
edition.workspace = true

[dependencies]
sez-core = { path = "../sez-core" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
sez-core = { path = "../sez-core", features = ["test-util"] }
serde_json = "1"
